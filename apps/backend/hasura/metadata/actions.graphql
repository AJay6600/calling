type Mutation {
  placeSingleCall(
    agentId: String!
    receiverPhoneNumber: String!
  ): PlaceSingleCallOutput
}

type PlaceSingleCallOutput {
  success: Boolean!
  executionId: String
  message: String
}

